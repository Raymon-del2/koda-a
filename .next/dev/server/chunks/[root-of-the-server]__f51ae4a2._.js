module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:assert [external] (node:assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:assert", () => require("node:assert"));

module.exports = mod;
}),
"[externals]/node:http [external] (node:http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http", () => require("node:http"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/node:net [external] (node:net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:net", () => require("node:net"));

module.exports = mod;
}),
"[externals]/node:buffer [external] (node:buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:buffer", () => require("node:buffer"));

module.exports = mod;
}),
"[externals]/node:util [external] (node:util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util", () => require("node:util"));

module.exports = mod;
}),
"[externals]/node:querystring [external] (node:querystring, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:querystring", () => require("node:querystring"));

module.exports = mod;
}),
"[externals]/node:events [external] (node:events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:events", () => require("node:events"));

module.exports = mod;
}),
"[externals]/node:zlib [external] (node:zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:zlib", () => require("node:zlib"));

module.exports = mod;
}),
"[externals]/node:perf_hooks [external] (node:perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:perf_hooks", () => require("node:perf_hooks"));

module.exports = mod;
}),
"[externals]/node:util/types [external] (node:util/types, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util/types", () => require("node:util/types"));

module.exports = mod;
}),
"[externals]/node:worker_threads [external] (node:worker_threads, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:worker_threads", () => require("node:worker_threads"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:diagnostics_channel [external] (node:diagnostics_channel, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:diagnostics_channel", () => require("node:diagnostics_channel"));

module.exports = mod;
}),
"[externals]/node:tls [external] (node:tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:tls", () => require("node:tls"));

module.exports = mod;
}),
"[externals]/node:http2 [external] (node:http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http2", () => require("node:http2"));

module.exports = mod;
}),
"[externals]/string_decoder [external] (string_decoder, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("string_decoder", () => require("string_decoder"));

module.exports = mod;
}),
"[externals]/node:url [external] (node:url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:url", () => require("node:url"));

module.exports = mod;
}),
"[externals]/node:async_hooks [external] (node:async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:async_hooks", () => require("node:async_hooks"));

module.exports = mod;
}),
"[externals]/node:console [external] (node:console, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:console", () => require("node:console"));

module.exports = mod;
}),
"[externals]/node:dns [external] (node:dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:dns", () => require("node:dns"));

module.exports = mod;
}),
"[project]/lib/qdrant.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "COLLECTIONS",
    ()=>COLLECTIONS,
    "addChatHistory",
    ()=>addChatHistory,
    "addKnowledgeItem",
    ()=>addKnowledgeItem,
    "addUserFact",
    ()=>addUserFact,
    "deleteFromCollection",
    ()=>deleteFromCollection,
    "deleteKnowledgeItem",
    ()=>deleteKnowledgeItem,
    "initializeQdrantCollections",
    ()=>initializeQdrantCollections,
    "qdrantClient",
    ()=>qdrantClient,
    "searchChatHistory",
    ()=>searchChatHistory,
    "searchKnowledge",
    ()=>searchKnowledge,
    "searchUserFacts",
    ()=>searchUserFacts
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$qdrant$2f$js$2d$client$2d$rest$2f$dist$2f$esm$2f$qdrant$2d$client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@qdrant/js-client-rest/dist/esm/qdrant-client.js [app-route] (ecmascript)");
;
// Qdrant Cloud configuration
const QDRANT_URL = process.env.QDRANT_URL || 'https://cfbea264-4259-432f-b479-7ecbb21e36d6.europe-west3-0.gcp.cloud.qdrant.io';
const QDRANT_API_KEY = process.env.QDRANT_API_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJhY2Nlc3MiOiJtIn0.vxDcVmUJFpFxNj1IXn64Rhso3nncYsArieuhU_PCgNc';
const qdrantClient = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$qdrant$2f$js$2d$client$2d$rest$2f$dist$2f$esm$2f$qdrant$2d$client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["QdrantClient"]({
    url: QDRANT_URL,
    apiKey: QDRANT_API_KEY
});
const COLLECTIONS = {
    KNOWLEDGE_BASE: 'koda_knowledge',
    CHAT_HISTORY: 'chat_history',
    USER_PROFILES: 'user_profiles'
};
async function initializeQdrantCollections() {
    try {
        const collections = await qdrantClient.getCollections();
        const existingCollections = collections.collections.map((c)=>c.name);
        // Create knowledge_base collection
        if (!existingCollections.includes(COLLECTIONS.KNOWLEDGE_BASE)) {
            await qdrantClient.createCollection(COLLECTIONS.KNOWLEDGE_BASE, {
                vectors: {
                    size: 768,
                    distance: 'Cosine'
                },
                optimizers_config: {
                    default_segment_number: 2
                }
            });
            console.log('✓ Created knowledge_base collection');
        }
        // Create chat_history collection
        if (!existingCollections.includes(COLLECTIONS.CHAT_HISTORY)) {
            await qdrantClient.createCollection(COLLECTIONS.CHAT_HISTORY, {
                vectors: {
                    size: 768,
                    distance: 'Cosine'
                },
                optimizers_config: {
                    default_segment_number: 2
                }
            });
            console.log('✓ Created chat_history collection');
        }
        // Create user_profiles collection
        if (!existingCollections.includes(COLLECTIONS.USER_PROFILES)) {
            await qdrantClient.createCollection(COLLECTIONS.USER_PROFILES, {
                vectors: {
                    size: 768,
                    distance: 'Cosine'
                },
                optimizers_config: {
                    default_segment_number: 2
                }
            });
            console.log('✓ Created user_profiles collection');
        }
        return true;
    } catch (error) {
        console.error('Error initializing Qdrant collections:', error);
        return false;
    }
}
async function addKnowledgeItem(id, content, vector, metadata) {
    try {
        await qdrantClient.upsert(COLLECTIONS.KNOWLEDGE_BASE, {
            points: [
                {
                    id,
                    vector,
                    payload: {
                        content,
                        ...metadata
                    }
                }
            ]
        });
        return true;
    } catch (error) {
        console.error('✗ Failed to add knowledge:', error);
        throw error;
    }
}
async function searchKnowledge(queryVector, limit = 5, category) {
    try {
        const filter = category ? {
            must: [
                {
                    key: 'category',
                    match: {
                        value: category
                    }
                }
            ]
        } : undefined;
        const results = await qdrantClient.search(COLLECTIONS.KNOWLEDGE_BASE, {
            vector: queryVector,
            limit,
            filter,
            with_payload: true
        });
        return results.map((hit)=>({
                id: hit.id,
                title: hit.payload?.title || 'Untitled',
                content: hit.payload?.content || '',
                parentContent: hit.payload?.parentContent || '',
                category: hit.payload?.category || 'general',
                source: hit.payload?.source || '',
                headings: hit.payload?.headings || [],
                score: hit.score || 0
            }));
    } catch (error) {
        // If collection doesn't exist, return empty results
        if (error.status === 404 || error.message?.includes('Not Found')) {
            console.log('Knowledge base collection not found, returning empty results');
            return [];
        }
        console.error('✗ Failed to search knowledge:', error);
        return [];
    }
}
async function deleteKnowledgeItem(id) {
    try {
        await qdrantClient.delete(COLLECTIONS.KNOWLEDGE_BASE, {
            points: [
                id
            ]
        });
        return true;
    } catch (error) {
        console.error('✗ Failed to delete knowledge:', error);
        throw error;
    }
}
async function addChatHistory(id, userId, chatId, content, vector, metadata) {
    try {
        await qdrantClient.upsert(COLLECTIONS.CHAT_HISTORY, {
            points: [
                {
                    id,
                    vector,
                    payload: {
                        user_id: userId,
                        chat_id: chatId,
                        content,
                        ...metadata,
                        timestamp: Date.now()
                    }
                }
            ]
        });
        return true;
    } catch (error) {
        console.error('✗ Failed to add chat history:', error);
        throw error;
    }
}
async function searchChatHistory(userId, queryVector, limit = 10, chatId) {
    try {
        const mustConditions = [
            {
                key: 'user_id',
                match: {
                    value: userId
                }
            }
        ];
        if (chatId) {
            mustConditions.push({
                key: 'chat_id',
                match: {
                    value: chatId
                }
            });
        }
        const results = await qdrantClient.search(COLLECTIONS.CHAT_HISTORY, {
            vector: queryVector,
            limit,
            filter: {
                must: mustConditions
            },
            with_payload: true
        });
        return results.map((hit)=>({
                id: hit.id,
                chatId: hit.payload?.chat_id,
                content: hit.payload?.content || '',
                role: hit.payload?.role,
                messageIndex: hit.payload?.messageIndex,
                isSummary: hit.payload?.isSummary || false,
                timestamp: hit.payload?.timestamp,
                score: hit.score || 0
            }));
    } catch (error) {
        // If collection doesn't exist, return empty results
        if (error.status === 404 || error.message?.includes('Not Found')) {
            console.log('Chat history collection not found, returning empty results');
            return [];
        }
        console.error('✗ Failed to search chat history:', error);
        return [];
    }
}
async function addUserFact(id, userId, fact, vector, metadata) {
    try {
        await qdrantClient.upsert(COLLECTIONS.USER_PROFILES, {
            points: [
                {
                    id,
                    vector,
                    payload: {
                        user_id: userId,
                        fact,
                        ...metadata,
                        timestamp: Date.now()
                    }
                }
            ]
        });
        return true;
    } catch (error) {
        console.error('✗ Failed to add user fact:', error);
        throw error;
    }
}
async function searchUserFacts(userId, queryVector, limit = 5, category) {
    try {
        const mustConditions = [
            {
                key: 'user_id',
                match: {
                    value: userId
                }
            }
        ];
        if (category) {
            mustConditions.push({
                key: 'category',
                match: {
                    value: category
                }
            });
        }
        const results = await qdrantClient.search(COLLECTIONS.USER_PROFILES, {
            vector: queryVector,
            limit,
            filter: {
                must: mustConditions
            },
            with_payload: true
        });
        return results.map((hit)=>({
                id: hit.id,
                fact: hit.payload?.fact || '',
                category: hit.payload?.category || 'general',
                confidence: hit.payload?.confidence || 0.5,
                extractedFrom: hit.payload?.extractedFrom || '',
                timestamp: hit.payload?.timestamp,
                score: hit.score || 0
            }));
    } catch (error) {
        // If collection doesn't exist, return empty results
        if (error.status === 404 || error.message?.includes('Not Found')) {
            console.log('User profiles collection not found, returning empty results');
            return [];
        }
        console.error('✗ Failed to search user facts:', error);
        return [];
    }
}
async function deleteFromCollection(collectionName, id) {
    try {
        await qdrantClient.delete(collectionName, {
            points: [
                id
            ]
        });
        return true;
    } catch (error) {
        console.error(`✗ Failed to delete from ${collectionName}:`, error);
        throw error;
    }
}
}),
"[project]/lib/embeddings.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Generate embeddings using a simple hash-based approach as fallback
 * This creates deterministic pseudo-embeddings for text similarity
 */ __turbopack_context__.s([
    "cosineSimilarity",
    ()=>cosineSimilarity,
    "generateEmbedding",
    ()=>generateEmbedding,
    "generateEmbeddingWithRetry",
    ()=>generateEmbeddingWithRetry,
    "generateEmbeddingsBatch",
    ()=>generateEmbeddingsBatch
]);
async function generateEmbedding(text) {
    try {
        // Try OpenAI first if key is available
        const openaiKey = process.env.OPENAI_API_KEY;
        if (openaiKey) {
            const response = await fetch('https://api.openai.com/v1/embeddings', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${openaiKey}`
                },
                body: JSON.stringify({
                    model: 'text-embedding-3-small',
                    input: text.slice(0, 8000)
                })
            });
            if (response.ok) {
                const data = await response.json();
                return data.data[0].embedding;
            }
        }
        // Fallback: Generate deterministic pseudo-embedding
        return generatePseudoEmbedding(text);
    } catch (error) {
        console.error('✗ Failed to generate embedding:', error);
        return generatePseudoEmbedding(text);
    }
}
/**
 * Generate a deterministic pseudo-embedding from text
 * Uses simple hashing to create a 768-dim vector
 */ function generatePseudoEmbedding(text) {
    const dimension = 768;
    const embedding = new Array(dimension).fill(0);
    // Simple hash-based embedding generation
    for(let i = 0; i < text.length; i++){
        const charCode = text.charCodeAt(i);
        embedding[i % dimension] += charCode / 255;
    }
    // Normalize
    const magnitude = Math.sqrt(embedding.reduce((sum, val)=>sum + val * val, 0));
    if (magnitude > 0) {
        for(let i = 0; i < dimension; i++){
            embedding[i] = embedding[i] / magnitude;
        }
    }
    return embedding;
}
async function generateEmbeddingWithRetry(text, maxRetries = 3) {
    for(let i = 0; i < maxRetries; i++){
        try {
            const embedding = await generateEmbedding(text);
            // Check if we got a valid embedding (not all zeros)
            if (embedding.some((v)=>v !== 0)) {
                return embedding;
            }
            // Wait before retry
            await new Promise((resolve)=>setTimeout(resolve, 1000 * (i + 1)));
        } catch (error) {
            if (i === maxRetries - 1) throw error;
        }
    }
    return new Array(768).fill(0);
}
async function generateEmbeddingsBatch(texts) {
    const embeddings = [];
    // Process in batches to avoid rate limits
    const batchSize = 5;
    for(let i = 0; i < texts.length; i += batchSize){
        const batch = texts.slice(i, i + batchSize);
        const batchEmbeddings = await Promise.all(batch.map((text)=>generateEmbeddingWithRetry(text)));
        embeddings.push(...batchEmbeddings);
    }
    return embeddings;
}
function cosineSimilarity(a, b) {
    if (a.length !== b.length) {
        throw new Error('Vectors must have the same dimensions');
    }
    let dotProduct = 0;
    let normA = 0;
    let normB = 0;
    for(let i = 0; i < a.length; i++){
        dotProduct += a[i] * b[i];
        normA += a[i] * a[i];
        normB += b[i] * b[i];
    }
    if (normA === 0 || normB === 0) return 0;
    return dotProduct / (Math.sqrt(normA) * Math.sqrt(normB));
}
}),
"[project]/lib/youtube.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "formatPublishedDate",
    ()=>formatPublishedDate,
    "formatViewCount",
    ()=>formatViewCount,
    "searchYouTube",
    ()=>searchYouTube
]);
/**
 * YouTube Data API Integration
 * Search for videos and display as cards
 */ const YOUTUBE_API_KEY = process.env.YOUTUBE_API_KEY || 'AIzaSyA82ZQFsZYuf_yzCsd4QN0tkpRMvKcs6EA';
const YOUTUBE_API_BASE = 'https://www.googleapis.com/youtube/v3';
async function searchYouTube(query, maxResults = 2) {
    try {
        const params = new URLSearchParams({
            part: 'snippet',
            q: query,
            type: 'video',
            maxResults: maxResults.toString(),
            key: YOUTUBE_API_KEY,
            order: 'relevance'
        });
        const response = await fetch(`${YOUTUBE_API_BASE}/search?${params.toString()}`);
        if (!response.ok) {
            throw new Error(`YouTube API error: ${response.status}`);
        }
        const data = await response.json();
        if (!data.items || data.items.length === 0) {
            return [];
        }
        // Get video IDs for statistics
        const videoIds = data.items.map((item)=>item.id.videoId).join(',');
        // Fetch statistics for view counts
        const statsResponse = await fetch(`${YOUTUBE_API_BASE}/videos?part=statistics&id=${videoIds}&key=${YOUTUBE_API_KEY}`);
        const statsData = statsResponse.ok ? await statsResponse.json() : {
            items: []
        };
        const statsMap = new Map(statsData.items?.map((item)=>[
                item.id,
                item.statistics
            ]) || []);
        return data.items.map((item)=>{
            const stats = statsMap.get(item.id.videoId);
            return {
                id: item.id.videoId,
                title: item.snippet.title,
                description: item.snippet.description,
                thumbnail: item.snippet.thumbnails.medium?.url || item.snippet.thumbnails.default?.url,
                channelTitle: item.snippet.channelTitle,
                publishedAt: item.snippet.publishedAt,
                viewCount: stats?.viewCount
            };
        });
    } catch (error) {
        console.error('✗ YouTube search failed:', error);
        return [];
    }
}
function formatViewCount(count) {
    if (!count) return '';
    const num = parseInt(count, 10);
    if (num >= 1000000) {
        return `${(num / 1000000).toFixed(1)}M`;
    }
    if (num >= 1000) {
        return `${(num / 1000).toFixed(1)}K`;
    }
    return count;
}
function formatPublishedDate(dateString) {
    const date = new Date(dateString);
    const now = new Date();
    const diff = now.getTime() - date.getTime();
    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    if (days === 0) return 'Today';
    if (days === 1) return 'Yesterday';
    if (days < 7) return `${days} days ago`;
    if (days < 30) return `${Math.floor(days / 7)} weeks ago`;
    if (days < 365) return `${Math.floor(days / 30)} months ago`;
    return `${Math.floor(days / 365)} years ago`;
}
}),
"[project]/app/api/chat/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "POST",
    ()=>POST,
    "maxDuration",
    ()=>maxDuration
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$groq$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@ai-sdk/groq/dist/index.mjs [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@ai-sdk/openai/dist/index.mjs [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/ai/dist/index.mjs [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$qdrant$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/qdrant.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$embeddings$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/embeddings.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$youtube$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/youtube.ts [app-route] (ecmascript)");
;
;
;
;
;
;
const maxDuration = 60;
// 2. Initialize providers
const groqProvider = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$groq$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createGroq"])({
    apiKey: process.env.GROQ_API_KEY || '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'
});
const nyatiCore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$ai$2d$sdk$2f$openai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createOpenAI"])({
    baseURL: 'https://ryan33121-nyati-core-api.hf.space/v1',
    apiKey: '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'
});
const SYSTEM_PROMPT = `### IDENTITY
You are Koda-A, the world's most proficient AI Architect. You are part of the Nyati Ecosystem. Your goal is to provide "Steel" quality solutions that are concise, accurate, and innovative.

### OPERATING PRINCIPLES
1. BE CONCISE: Never explain what the user already knows. Show code, don't talk about it.
2. NO APOLOGIES: If there is an error, fix it. Do not apologize for being an AI.
3. THINK FIRST: Before providing a solution, provide a 1-sentence "Mental Model" of how you will solve it.
4. FORMATTING: Always use clean Markdown. Use specific filenames in code blocks like: \`\`\`tsx file="app/page.tsx"

### THINKING FORMAT (CRITICAL)
Before you reply to the user, you MUST think about your approach. Wrap your entire mental model, internal reasoning, and step-by-step thinking inside <thinking> and </thinking> XML tags. Your actual, final response to the user must go outside and after these tags.

Example:
<thinking>
The user is asking about React hooks. I should explain useEffect first, then provide a concrete example with cleanup. This follows the "Mental Model then Code" principle.
</thinking>

Here's how useEffect works...

### ARCHITECT GUIDELINES
- When coding, prioritize Next.js App Router, Tailwind CSS, and Shadcn UI.
- Always address the root cause of a logic error.
- If a task is complex, break it into "Phases" (Phase 1: Logic, Phase 2: UI).
- Write full, copy-pasteable files. Never write "snippets" that require the user to guess where code goes.

### TONE
Professional, direct, and elite. You are a high-end tool, not a servant.

You have access to the YouTube Data API. If a user asks you to find a video, search for a channel, or look up something on YouTube:
1. Provide a helpful textual response.
2. At the very end of your message, include a search command like this: [YT_SEARCH: "the search terms"] (e.g., [YT_SEARCH: "MrBeast latest video"]).
3. Use this to help the user find specific content they are looking for. I will handle the fetching and display of the actual results.
4. If you aren't sure about a channel, use this search command to find it!`;
// RAG-enhanced system prompt builder
async function buildRAGSystemPrompt(userQuery, userId, searchMode) {
    let ragContext = '';
    let sources = [];
    let youtubeVideos = [];
    try {
        // Generate embedding for the query
        const queryEmbedding = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$embeddings$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["generateEmbeddingWithRetry"])(userQuery);
        // Search knowledge base (always do this)
        const knowledgeResults = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$qdrant$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["searchKnowledge"])(queryEmbedding, 3);
        if (knowledgeResults.length > 0) {
            sources = knowledgeResults.map((result, idx)=>({
                    id: idx + 1,
                    title: result.title,
                    url: result.source || '',
                    type: 'qdrant',
                    snippet: result.content.slice(0, 200)
                }));
            ragContext += '\n\n### RELEVANT KNOWLEDGE (with citations)\n';
            knowledgeResults.forEach((result, idx)=>{
                // Use parent content for more context, fallback to child content
                const contextText = result.parentContent || result.content;
                ragContext += `[${idx + 1}] ${result.title}${result.source ? ` [Source: ${result.source}]` : ''}:\n${contextText}\n\n`;
            });
            // Add citation instruction
            ragContext += '\n### CITATION INSTRUCTION\n';
            ragContext += 'When you use information from the sources above, cite them with [1], [2], etc. at the end of the relevant sentence.\n';
        }
        // DEEP SEARCH: YouTube search
        if (searchMode === 'deep-search') {
            console.log('🔍 Deep search: Searching YouTube...');
            const youtubeResults = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$youtube$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["searchYouTube"])(userQuery, 2);
            if (youtubeResults.length > 0) {
                youtubeVideos = youtubeResults;
                // Add YouTube sources to the list
                const youtubeStartIdx = sources.length;
                youtubeResults.forEach((video, idx)=>{
                    sources.push({
                        id: youtubeStartIdx + idx + 1,
                        title: video.title,
                        url: `https://youtube.com/watch?v=${video.id}`,
                        type: 'youtube',
                        snippet: video.description.slice(0, 200)
                    });
                });
                ragContext += '\n\n### RELEVANT YOUTUBE VIDEOS\n';
                youtubeResults.forEach((video, idx)=>{
                    const sourceNum = youtubeStartIdx + idx + 1;
                    ragContext += `[${sourceNum}] ${video.title} by ${video.channelTitle}:\n${video.description}\n\n`;
                });
            }
        }
        // Search user facts for personalization if userId provided
        if (userId) {
            const userFacts = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$qdrant$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["searchUserFacts"])(userId, queryEmbedding, 3);
            if (userFacts.length > 0) {
                ragContext += '\n\n### USER PREFERENCES\n';
                userFacts.forEach((fact)=>{
                    ragContext += `- ${fact.fact}\n`;
                });
            }
            // Search chat history for long-term memory
            const chatHistory = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$qdrant$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["searchChatHistory"])(userId, queryEmbedding, 5);
            if (chatHistory.length > 0) {
                ragContext += '\n\n### RELEVANT PAST CONVERSATIONS\n';
                chatHistory.slice(0, 3).forEach((msg, idx)=>{
                    ragContext += `[${idx + 1}] ${msg.role === 'user' ? 'User' : 'Assistant'}: ${msg.content.substring(0, 200)}...\n`;
                });
            }
        }
    } catch (error) {
        console.error('RAG retrieval error:', error);
    }
    return {
        prompt: SYSTEM_PROMPT + ragContext,
        sources,
        youtubeVideos
    };
}
async function POST(req) {
    const { messages, model, userId, searchMode } = await req.json();
    // Get the latest user message for RAG
    const latestMessage = messages[messages.length - 1];
    const userQuery = latestMessage?.content || '';
    // Build RAG-enhanced system prompt and get metadata
    const { prompt: enhancedSystemPrompt, sources, youtubeVideos } = await buildRAGSystemPrompt(userQuery, userId, searchMode);
    // Create a stream that includes metadata first, then the AI response
    const stream = new ReadableStream({
        async start (controller) {
            // Send metadata as first chunk
            const metadata = JSON.stringify({
                type: 'metadata',
                sources,
                youtubeVideos
            });
            controller.enqueue(new TextEncoder().encode(metadata + '\n'));
            try {
                let result;
                // DUAL-AGENT WORKFLOW: If user selected medium, use fast for thinking, slow for generation
                if (model === 'medium') {
                    console.log('🧠 Dual-agent: Fast agent thinking, slow agent generating...');
                    // Step 1: Fast agent generates a plan/thinking
                    const thinkingResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["streamText"])({
                        model: groqProvider.languageModel('llama-3.1-8b-instant'),
                        system: enhancedSystemPrompt,
                        messages: [
                            ...messages,
                            {
                                role: 'user',
                                content: 'Generate a brief plan/approach for answering this. Keep it under 100 words.'
                            }
                        ]
                    });
                    let thinkingText = '';
                    for await (const chunk of thinkingResult.textStream){
                        thinkingText += chunk;
                    }
                    console.log('📝 Plan generated, now using slow agent to respond...');
                    // Step 2: Use slow agent (nyati) with optimized settings for generation
                    result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["streamText"])({
                        model: nyatiCore.languageModel('llama3.2:1b'),
                        system: enhancedSystemPrompt + '\n\n### GENERATION GUIDELINES\nRespond quickly and concisely. Use the following plan to guide your response:\n' + thinkingText,
                        messages,
                        temperature: 0.7
                    });
                } else if (model === 'groq' || model === 'pro') {
                    // Fast agent only (Koda-A)
                    console.log('Using Groq API (fast)...');
                    result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["streamText"])({
                        model: groqProvider.languageModel('llama-3.1-8b-instant'),
                        system: enhancedSystemPrompt,
                        messages
                    });
                } else {
                    // Slow agent only (nyati-core)
                    console.log('Using HF API (nyati slow)...');
                    result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$ai$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["streamText"])({
                        model: nyatiCore.languageModel('llama3.2:1b'),
                        system: enhancedSystemPrompt,
                        messages,
                        temperature: 0.7
                    });
                }
                // Stream the text chunks
                for await (const chunk of result.textStream){
                    try {
                        controller.enqueue(new TextEncoder().encode(chunk));
                    } catch (enqueueError) {
                        break;
                    }
                }
                try {
                    controller.close();
                } catch (closeError) {
                // Controller already closed, ignore
                }
            } catch (error) {
                console.error('Streaming error:', error);
                controller.error(error);
            }
        }
    });
    return new Response(stream, {
        headers: {
            'Content-Type': 'text/plain; charset=utf-8'
        }
    });
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__f51ae4a2._.js.map